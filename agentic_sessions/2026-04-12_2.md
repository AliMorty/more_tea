# Session Summary — 2026-04-12 (2)

## High-level Summary

Made three small visual tweaks to the site:

1. **Font change** — Switched headings and key UI elements (nav title, footer brand, section labels, etc.) to Calisto MT, with Playfair Display and Georgia as fallbacks. Calisto MT is a system font so it renders on Windows/Mac without any additional loading.
2. **"Join Us Links" label font** — Applied the same `--font-heading` (Calisto MT) to the "Join Us Links" panel label for consistency.
3. **Join panel width** — Widened the join panel column from `260px` to `300px` so the "Add to Google Calendar" button fits on a single line.

## Technical Details

**File modified:** `docs/css/style.css`

- `--font-heading` variable updated: `'Calisto MT', 'Playfair Display', Georgia, serif`
- `.join-panel-label` — added `font-family: var(--font-heading)`
- `.cal-join-inner` grid column changed from `1fr 260px` to `1fr 300px`
